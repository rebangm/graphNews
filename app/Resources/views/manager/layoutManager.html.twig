{% extends "::manager/baseManager.html.twig" %}

{% block body %}
<div class="navbar navbar-inverse navbar-fixed-top">

        <div class="container-fluid">
            <button type="button" class="btn btn-default navbar-btn navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="navbar-collapse collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">

                    <li class="{% if app.request.get('_route') matches '/.*_admin_homepage/' %}active{% endif %}"><a href="{{path('graph_news_front_homepage')}}">Home</a></li>
                    <li class="{% if app.request.get('_route') matches '/.*_user_manage.*/' %}active{% endif %}"><a href="{{path('graphnews_user_manage')}}">Users</a></li>
                    <li class="{% if app.request.get('_route') matches '/.*_admin_site.*/' %}active{% endif %}"><a href="{{path('graph_news_admin_sitelist')}}">Sites</a></li>
                    <li class="{% if app.request.get('_route') matches '/.*_admin_parser.*/' %}active{% endif %}"><a href="{{path('graph_news_admin_parserlist')}}">Parser</a></li>


                    
                </ul>

                <form class="navbar-form navbar-right">
                    <a href="{{ path('graphnews_user_logout') }}" class="btn btn-default">logout</a>
                </form>
                <div class="navbar-right"><p class="user-info"><i class="glyphicon glyphicon-user"></i> Welcome {{ app.user.username }}</p></div>
            </div><!--/.nav-collapse -->
        </div>

</div>

<div class="container-fluid">
    {% block total %}
        <div class="row">
            <div id="leftMenu" class="col-md-2">
                <h3>GraphNews</h3>
                {% block leftmenu %}
                    <ul class="nav nav-pills nav-stacked">
                        <li><a href="{{ path('graph_news_admin_homepage') }}">Accueil de l'application</a></li>
                    </ul>
                {% endblock %}
            </div>
            <div id="content" class="col-md-10">
                {% block content %}
                {% endblock %}
            </div>
        </div>
    {% endblock %}
    <hr>
</div>
{% endblock %}
